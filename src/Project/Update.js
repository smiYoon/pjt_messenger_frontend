import styles from './Update.module.css';

console.groupCollapsed('src/Project/Update.js'); console.groupEnd();

const Update = () => {
    console.group('Update() invoked.'); console.groupEnd();
    return (
        <div>Update
        </div>
    );
}


export default Update;
